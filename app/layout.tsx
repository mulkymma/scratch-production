import { Analytics } from '@vercel/analytics/next'
import type { Metadata, Viewport } from 'next'
import './globals.css'

export const metadata: Metadata = {
  title: 'Scratch Production — Creating. Capturing. Connecting.',
  description: 'Scratch Production is a Mombasa-based creative media studio for photography, content creation, and social media storytelling.',
  generator: 'v0.app',
  icons: {
    icon: '/scratch_logo-removebg-preview.png',
    apple: '/scratch_logo-removebg-preview.png',
  },
}

export const viewport: Viewport = {
  colorScheme: 'light dark',
  themeColor: [
    { media: '(prefers-color-scheme: light)', color: 'white' },
    { media: '(prefers-color-scheme: dark)', color: 'black' },
  ],
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className="antialiased">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
